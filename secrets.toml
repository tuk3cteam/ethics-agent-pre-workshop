# (A) Inference API 사용 시: 별도 엔드포인트 없이 이 값만 있으면 됩니다.
#    모델 ID는 코드 기본값 dnotitia/DNA-R1 사용

# (B) TGI/Endpoint 사용 시: 여기에 엔드포인트를 지정하세요(예: https://{your-endpoint}/)
DNA_R1_ENDPOINT = "https://{your-tgi-endpoint}"  # 끝에 / 없어도 됩니다.
DNA_BACKEND = "hf-api"  # "hf-api" | "tgi" | "local" 중 선택 (미지정 시 UI에서 선택)

# (로컬 GPU 모드에서만 필요) 모델 ID 커스터마이즈 가능
DNA_R1_MODEL_ID = "dnotitia/DNA-R1"
